org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError -Dkotlin.daemon.jvm.options=-Xmx2G --add-opens=java.base/java.lang.ref=ALL-UNNAMED -Dhttps.protocols=TLSv1.2,TLSv1.1,TLSv1
android.useAndroidX=true
android.enableJetifier=true
org.gradle.internal.repository.max.retries=10
org.gradle.internal.repository.initial.backoff=500
# Speed and memory
# If you face SSL issues with Google Maven, you can enable this to prefer mirrors (set in settings.gradle)

# Network timeout configurations
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=false
org.gradle.vfs.watch=false
kotlin.incremental=true
org.gradle.workers.max=4
org.gradle.configureondemand=false

# Network settings for better dependency resolution
systemProp.http.connectionTimeout=60000
systemProp.http.socketTimeout=60000
systemProp.https.connectionTimeout=60000
systemProp.https.socketTimeout=60000

# Added to bypass SSL handshake issues temporarily
systemProp.javax.net.ssl.trustStoreType=JKS
systemProp.javax.net.ssl.trustStore=/etc/ssl/certs/java/cacerts
systemProp.javax.net.ssl.trustStorePassword=changeit

# TLS protocol configuration to fix download issues
systemProp.https.protocols=TLSv1.2,TLSv1.1,TLSv1

# Configuration cache disabled to avoid caching issues with Kotlin
